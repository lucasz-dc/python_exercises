number_one = float(input('Nota da primeira prova: '))
number_two = float(input('Nota da segunda prova: '))

med = (number_one + number_two) / 2

print('A média das notas das duas provas foi {:.1f}.'.format(med))
